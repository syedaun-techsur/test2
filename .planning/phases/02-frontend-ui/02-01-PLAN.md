---
phase: 02-frontend-ui
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true
user_setup: []

# Feature traceability (link to original requirements)
features:
  implements: ["CREATE-01", "COMPLETE-01", "DELETE-01"]
  depends_on: []
  enables: []

must_haves:
  truths:
    - "User sees a text input to create new tasks"
    - "User sees a list of all existing tasks"
    - "User can click a checkbox to mark tasks as complete"
    - "User can click a delete button to remove tasks"
    - "Frontend communicates correctly with backend API endpoints"
  artifacts:
    - path: "client/src/App.jsx"
      provides: "Main app component with state and API calls"
      exports: ["tasks state", "fetchTasks", "addTask", "toggleComplete", "deleteTask"]
    - path: "client/src/TaskList.jsx"
      provides: "Task list rendering"
      min_lines: 15
    - path: "client/src/TaskItem.jsx"
      provides: "Individual task with checkbox and delete"
      min_lines: 15
    - path: "client/src/TaskForm.jsx"
      provides: "Task creation form"
      min_lines: 20
    - path: "client/src/App.css"
      provides: "Styling for all components"
      min_lines: 50
    - path: "client/vite.config.js"
      provides: "Vite config with API proxy"
      contains: "proxy to localhost:3001"
  key_links:
    - from: "client/src/App.jsx"
      to: "http://localhost:3001/api"
      via: "fetch calls"
      pattern: "fetch.*api/tasks"
---

<objective>
Create React frontend with create, complete, and delete functionality

Purpose: Provide user interface to interact with tasks - creating new tasks, marking complete, and deleting

Output: Working React app at client/ with all components and styling
</objective>

<feature_dependencies>
Implements: CREATE-01: User can add new tasks, COMPLETE-01: User can mark tasks as done, DELETE-01: User can remove tasks
Depends on: None (Phase 1 API - backend must be running)
Enables: All v1 requirements complete
</feature_dependencies>

<context>
@.planning/ROADMAP.md
@.planning/phases/02-frontend-ui/01-CONTEXT.md
@.planning/phases/02-frontend-ui/02-RESEARCH.md
@.planning/phases/01-backend-api/01-01-PLAN.md

# User decisions (LOCKED):
# - Flat component structure (no nested folders)
# - Files: App.jsx, TaskList.jsx, TaskItem.jsx, TaskForm.jsx, App.css
# - Plain CSS styling (no frameworks)
# - fetch API for HTTP
# - Local React useState for state

# Backend API (must be running):
# - GET http://localhost:3001/api/tasks - returns all tasks
# - POST http://localhost:3001/api/tasks - creates task
# - PATCH http://localhost:3001/api/tasks/:id - updates completion
# - DELETE http://localhost:3001/api/tasks/:id - deletes task
</context>

<tasks>

<task type="auto">
  <name>Initialize Vite + React project</name>
  <files>client/package.json</files>
  <action>
Create client/ directory and initialize Vite + React:

1. Create client/package.json with:
   - vite ^8.0.10
   - react ^19.2.5
   - @vitejs/plugin-react

2. Create vite.config.js with:
   - React plugin
   - Proxy to localhost:3001 for /api routes

3. Create client/index.html
4. Create client/src/main.jsx - React 19 entry point

Run: cd client && npm install
  </action>
  <verify>
ls client/package.json && ls client/vite.config.js && cat client/package.json | grep -q '"react"'</verify>
  <done>client/ directory exists with Vite + React dependencies and config</done>
</task>

<task type="auto">
  <name>Create App.jsx with state and API integration</name>
  <files>client/src/App.jsx</files>
  <action>
Create client/src/App.jsx:

1. Import: useState, useEffect from 'react'
2. API_BASE = 'http://localhost:3001/api' (or use proxy /api)

3. State:
   - tasks: Task[] - all tasks
   - loading: boolean - initial load state
   - error: string | null - error message

4. Functions:
   - fetchTasks() - GET /api/tasks, set tasks
   - addTask(text) - POST /api/tasks, add to list
   - toggleComplete(id, completed) - PATCH /api/tasks/:id
   - deleteTask(id) - DELETE /api/tasks/:id

5. useEffect: call fetchTasks() on mount

6. Render:
   - h1: "Todo App"
   - TaskForm(onAdd={addTask})
   - loading ? "Loading..." : TaskList(tasks, onToggle, onDelete)
   - error && <div className="error">{error}</div>

7. Handle API response format: { success: true, data: [...] }
  </action>
  <verify>
ls client/src/App.jsx && cat client/src/App.jsx | grep -q "useState"</verify>
  <done>App.jsx has state management, API calls, renders TaskForm and TaskList</done>
</task>

<task type="auto">
  <name>Create TaskForm component</name>
  <files>client/src/TaskForm.jsx</files>
  <action>
Create client/src/TaskForm.jsx:

1. Import: useState from 'react'
2. Props: { onAdd: (text: string) => void }

3. State:
   - text: string (input value)

4. handleSubmit(e):
   - e.preventDefault()
   - if (!text.trim()) return
   - onAdd(text.trim())
   - setText('')

5. Render:
   - form onSubmit={handleSubmit}
   - input value={text} onChange={e => setText(e.target.value)}
   - placeholder="Enter task..."
   - button type="submit" - "Add"

6. Auto-focus on input
  </action>
  <verify>
ls client/src/TaskForm.jsx && cat client/src/TaskForm.jsx | grep -q "useState"</verify>
  <done>TaskForm renders input and calls onAdd with trimmed text</done>
</task>

<task type="auto">
  <name>Create TaskList component</name>
  <files>client/src/TaskList.jsx</files>
  <action>
Create client/src/TaskList.jsx:

1. Props:
   - tasks: Task[]
   - onToggle: (id: string, completed: boolean) => void
   - onDelete: (id: string) => void

2. Render:
   - tasks.length === 0 ? <p>No tasks yet</p>
   - ul.task-list:
     - tasks.map(task => TaskItem(key=task.id, task, onToggle, onDelete))

3. Empty state handling
  </action>
  <verify>
ls client/src/TaskList.jsx && cat client/src/TaskList.jsx | grep -q "tasks.map"</verify>
  <done>TaskList renders tasks or empty message</done>
</task>

<task type="auto">
  <name>Create TaskItem component</name>
  <files>client/src/TaskItem.jsx</files>
  <action>
Create client/src/TaskItem.jsx:

1. Props:
   - task: Task { id, text, completed }
   - onToggle: (id: string, completed: boolean) => void
   - onDelete: (id: string) => void

2. Render:
   - li className={task.completed ? 'completed' : ''}
   - input type="checkbox"
   - checked={task.completed}
   - onChange={() => onToggle(task.id, !task.completed)}
   - span className="task-text">{task.text}</span>
   - button className="delete-btn" onClick={() => onDelete(task.id)}
     - "Delete"

3. Visual feedback for completed tasks (strikethrough or dimmed)
  </action>
  <verify>
ls client/src/TaskItem.jsx && cat client/src/TaskItem.jsx | grep -q "type=\"checkbox\""</verify>
  <done>TaskItem renders checkbox, text, and delete button</done>
</task>

<task type="auto">
  <name>Create CSS styles</name>
  <files>client/src/App.css</files>
  <action>
Create client/src/App.css:

1. Base styles:
   - body: font-family, margin, padding, background

2. App container:
   - max-width, margin auto, padding

3. Task form:
   - display flex, gap, input flex-grow, button styles

4. Task list:
   - list-style none, padding 0

5. Task item:
   - display flex, align-items center, gap
   - padding, margin-bottom, border

6. Completed state:
   - opacity: 0.6, text-decoration: line-through

7. Delete button:
   - margin-left auto, background red, color white

8. Error state:
   - color red, padding

9. Empty state:
   - color gray, text-align center
  </action>
  <verify>
ls client/src/App.css && wc -l client/src/App.css</verify>
  <done>App.css with complete styling</done>
</task>

</tasks>

<verification>
- Vite dev server starts on port 5173 without errors
- React app renders without console errors
- TaskForm input accepts text and calls API
- TaskList shows tasks or "No tasks yet"
- TaskItem checkbox toggles completion
- TaskItem delete button removes task
- Tasks persist after page refresh (API backend)
</verification>

<success_criteria>
1. User sees a text input to create new tasks
2. User sees a list of all existing tasks
3. User can click a checkbox to mark tasks as complete
4. User can click a delete button to remove tasks
5. Frontend communicates correctly with backend API endpoints
</success_criteria>

<output>
After completion, create `.planning/phases/02-frontend-ui/02-01-SUMMARY.md`
</output>